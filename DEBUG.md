# 🐛 Руководство по отладке

## Быстрая диагностика

### 1. Откройте консоль браузера
**Windows/Linux:** `F12` или `Ctrl+Shift+I`  
**Mac:** `Cmd+Option+I`

### 2. Проверьте сообщения
Ищите эти сообщения в консоли:

✅ **Успешная загрузка:**
```
🚀 Инициализация сайта...
✅ Частицы инициализированы
✅ Курсор инициализирован
✅ Мобильное меню инициализировано
✅ Языковая система инициализирована
✅ Переключатель темы инициализирован
✅ Эффекты прокрутки инициализированы
✅ Музыкальный плеер инициализирован
✅ Статистика инициализирована
✅ Погода загружена успешно
✅ Сайт загружен успешно!
```

⚠️ **Предупреждения (не критично):**
```
⚠️ Canvas не доступен, пропускаем частицы
⚠️ Погода не загрузилась: [причина]
ℹ️ Кастомный курсор отключен (тач-устройство)
```

❌ **Ошибки (требуют исправления):**
```
❌ Canvas не найден
❌ Ошибка загрузки погоды: [причина]
❌ Ошибка инициализации Audio: [причина]
```

## Типичные проблемы и решения

### Проблема 1: Белый экран
**Симптомы:** Страница пустая, ничего не видно

**Причины:**
1. Файл `style.css` не загружен
2. Неправильный путь к CSS
3. Ошибка в CSS файле

**Решение:**
1. Откройте консоль (F12)
2. Перейдите на вкладку "Network" (Сеть)
3. Обновите страницу (F5)
4. Найдите `style.css` в списке
5. Если статус 404 - файл не найден, проверьте загрузку
6. Если статус 200 - файл загружен, проверьте содержимое

### Проблема 2: JavaScript не работает
**Симптомы:** Нет анимаций, кнопки не работают, консоль пустая

**Причины:**
1. Файлы `script.js` или `i18n.js` не загружены
2. Ошибка в JavaScript коде
3. Неправильный порядок загрузки скриптов

**Решение:**
1. Откройте консоль (F12)
2. Проверьте красные ошибки
3. Убедитесь, что в конце `index.html` есть:
   ```html
   <script src="i18n.js"></script>
   <script src="script.js"></script>
   ```
4. Проверьте, что файлы загружены на сервер
5. Очистите кеш браузера (Ctrl+F5)

### Проблема 3: Музыка не воспроизводится
**Симптомы:** Плеер виден, но музыка не играет

**Причины:**
1. MP3 файлы не загружены
2. Неправильные имена файлов
3. Файлы слишком большие
4. Браузер блокирует автовоспроизведение

**Решение:**
1. Проверьте консоль на ошибки типа:
   ```
   GET https://sys.gamer.gd/music.mp3 404 (Not Found)
   ```
2. Убедитесь, что MP3 файлы в корневой папке
3. Проверьте имена файлов (должны совпадать с кодом)
4. Попробуйте открыть файл напрямую:
   ```
   https://sys.gamer.gd/stalk%20ur%20socials%20(slowed).mp3
   ```
5. Нажмите кнопку Play вручную (автовоспроизведение может быть заблокировано)

### Проблема 4: Погода не загружается
**Симптомы:** Виджет показывает "Загрузка..." или "Не удалось загрузить"

**Причины:**
1. InfinityFree блокирует внешние API (CORS)
2. API временно недоступен
3. Нет интернета

**Решение:**
1. Это **нормально** для InfinityFree
2. Проверьте консоль на ошибку:
   ```
   Access to fetch at 'https://api.open-meteo.com/...' has been blocked by CORS policy
   ```
3. Если видите эту ошибку - это ограничение хостинга
4. Варианты:
   - Оставить как есть (виджет покажет "Загрузка...")
   - Убрать виджет погоды из HTML
   - Использовать платный хостинг без CORS ограничений

### Проблема 5: Изображения не загружаются
**Симптомы:** Вместо аватаров/иконок пустые места

**Причины:**
1. Папки `avatars/` или `mods-icons/` не загружены
2. Неправильные имена файлов
3. Неправильные права доступа

**Решение:**
1. Проверьте консоль на ошибки 404
2. Убедитесь, что папки загружены на сервер
3. Проверьте структуру:
   ```
   htdocs/
   ├── avatars/
   │   ├── Sys.jpg
   │   ├── HeatTreak.jpg
   │   └── ...
   └── mods-icons/
       ├── enternal-model.png
       └── skinster.png
   ```
4. Проверьте права доступа (должны быть 644)

### Проблема 6: Кастомный курсор не работает
**Симптомы:** Обычный курсор вместо кастомного

**Причины:**
1. Вы на мобильном устройстве (курсор отключен автоматически)
2. Ошибка в JavaScript
3. CSS не загружен

**Решение:**
1. Проверьте, что вы на ПК (на телефоне курсор отключен)
2. Откройте консоль, должно быть:
   ```
   ✅ Курсор инициализирован
   ```
   или
   ```
   ℹ️ Кастомный курсор отключен (тач-устройство)
   ```
3. Если ошибка - проверьте, что `script.js` загружен

### Проблема 7: Мобильное меню не открывается
**Симптомы:** Гамбургер-кнопка не работает на телефоне

**Причины:**
1. JavaScript не загружен
2. Ошибка в коде
3. Конфликт с другими скриптами

**Решение:**
1. Откройте консоль на телефоне:
   - Android Chrome: Меню → Инструменты → Консоль
   - iOS Safari: Настройки → Safari → Дополнения → Web Inspector
2. Проверьте ошибки
3. Убедитесь, что в консоли есть:
   ```
   ✅ Мобильное меню инициализировано
   ```

### Проблема 8: Статистика не сохраняется
**Симптомы:** Счетчик посещений сбрасывается при обновлении

**Причины:**
1. localStorage заблокирован
2. Приватный режим браузера
3. Настройки безопасности

**Решение:**
1. Откройте `test.html` и проверьте localStorage
2. Если заблокирован - выйдите из приватного режима
3. Проверьте настройки браузера (cookies должны быть включены)

## Инструменты отладки

### 1. Тестовая страница
Откройте `https://sys.gamer.gd/test.html`

Проверьте все тесты:
- ✅ JavaScript работает
- ✅ localStorage доступен
- ✅ Canvas отображается
- ✅ Файлы загружаются
- ⚠️ API погоды (может не работать)

### 2. Консоль браузера
**Вкладки:**
- **Console** - сообщения и ошибки
- **Network** - загрузка файлов
- **Application** - localStorage, cookies
- **Elements** - HTML структура

**Полезные команды:**
```javascript
// Проверить localStorage
localStorage.getItem('visitCount')

// Проверить язык
localStorage.getItem('language')

// Проверить тему
localStorage.getItem('theme')

// Очистить localStorage
localStorage.clear()

// Проверить audio
audio.paused  // true если не играет
audio.currentTime  // текущее время
audio.duration  // длительность
```

### 3. Network вкладка
Показывает все загружаемые файлы:
- **200** - файл загружен успешно ✅
- **304** - файл из кеша ✅
- **404** - файл не найден ❌
- **403** - доступ запрещен ❌
- **500** - ошибка сервера ❌

## Сбор информации для поддержки

Если проблема не решается, соберите информацию:

### 1. Скриншот консоли
1. Откройте консоль (F12)
2. Сделайте скриншот всех сообщений
3. Особенно важны красные ошибки

### 2. Информация о браузере
```javascript
// Скопируйте и вставьте в консоль
console.log({
    userAgent: navigator.userAgent,
    language: navigator.language,
    online: navigator.onLine,
    cookies: navigator.cookieEnabled,
    screen: `${screen.width}x${screen.height}`,
    viewport: `${window.innerWidth}x${window.innerHeight}`
});
```

### 3. Проверка файлов
Откройте Network вкладку и проверьте:
- Какие файлы загружены (статус 200)
- Какие файлы не найдены (статус 404)
- Размеры файлов

### 4. Тест на другом устройстве
Попробуйте открыть сайт:
- На другом браузере (Chrome, Firefox, Safari)
- На другом устройстве (телефон, планшет)
- В приватном режиме

## Частые вопросы

**Q: Почему погода не работает?**  
A: InfinityFree блокирует внешние API. Это нормально, можно оставить или убрать виджет.

**Q: Почему музыка не автоматически играет?**  
A: Браузеры блокируют автовоспроизведение. Нужно нажать Play вручную.

**Q: Почему курсор обычный на телефоне?**  
A: Кастомный курсор автоматически отключается на тач-устройствах.

**Q: Почему сайт медленно загружается?**  
A: InfinityFree - бесплатный хостинг с ограничениями. Для быстрой загрузки нужен платный хостинг.

**Q: Можно ли использовать другой API для погоды?**  
A: Да, но большинство API будут заблокированы InfinityFree из-за CORS.

## Контакты

Если ничего не помогло:
1. Проверьте все пункты в `UPLOAD_CHECKLIST.md`
2. Запустите `test.html` и сделайте скриншот
3. Соберите информацию из консоли
4. Опишите проблему подробно

---

**Помните:** InfinityFree - бесплатный хостинг с ограничениями. Некоторые функции могут не работать из-за политик хостинга. Это нормально!
